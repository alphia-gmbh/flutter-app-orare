# Copyright 2023 Alphia GmbH
name: orare
description: 'Discover your healthy power of gratitude. Write daily and share joy.'
publish_to: 'none'
version: 2.2.2+36
# News: We’re always crafting new features and squishing bugs. Keep your app updated for a refreshing experience.

environment:
  sdk: ^3.8.1

dependencies:
  alphia_core:
    git:
      url: https://github.com/alphia-gmbh/flutter-package-core.git
      # path: alphia_core
      ref: main
  archive: ^4.0.1
  async: ^2.11.0
  cloud_firestore: ^5.0.1
  cloud_functions: ^5.0.1
  firebase_analytics: ^11.0.1
  firebase_app_check: ^0.3.0+4
  firebase_auth: ^5.1.0
  firebase_core: ^3.3.0
  firebase_crashlytics: ^4.0.1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router: ^16.0.0
  flutter_local_notifications: ^19.0.0
  flutter_secure_storage: ^9.0.0
  intl: any
  in_app_review: ^2.0.9
  material_symbols_icons: ^4.2801.0
  path_provider: ^2.1.0
  screenshot: ^3.0.0
  share_plus: ^11.0.0
  timezone: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
      # font-inter-regular.ttf
  fonts:
    - family: Roboto # Necessary for web version to avoid online request of Roboto as fallback font
      fonts:
        - asset: assets/font-inter-regular.ttf
